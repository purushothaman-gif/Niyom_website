/* Moved to shared/ so niyomwealth.com and the mobile app run one copy.
 * Kept here so existing imports keep working; import from shared/ in new code. */
export * from '../../../shared/portal/services/PaymentService';
