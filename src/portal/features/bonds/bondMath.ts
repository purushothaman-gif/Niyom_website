/**
 * Moved to `shared/portal/bonds/bondMath.ts` so the mobile app runs the same lot
 * rule and the same order breakdown as the website. Re-exported here so every
 * existing import path in `src/` keeps resolving.
 */
export * from '../../../../shared/portal/bonds/bondMath';
