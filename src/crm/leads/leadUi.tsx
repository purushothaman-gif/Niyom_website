/* Promoted to ../ui/kit so HR & Payroll can use the same primitives rather than
 * cloning them. Kept here so existing Lead imports keep working; import from
 * ../ui/kit in new code. */
export * from '../ui/kit';
