/* Moved to shared/ so niyomwealth.com and the mobile app build the same
 * referral URLs. Kept here so existing imports keep working. */
export * from '../../../shared/marketing/marketingConstants';
