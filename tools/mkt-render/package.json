{
  "name": "niyom-mkt-render",
  "version": "0.1.0",
  "private": true,
  "description": "Headless renderer for the automated daily content batch. Drives the CRM's own poster/video renderers in Chromium so auto content is drawn by the same code the studio uses.",
  "type": "module",
  "scripts": {
    "build:harness": "vite build",
    "spike": "npm run build:harness && tsx src/spike.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.3",
    "vite": "^5.4.2"
  }
}
